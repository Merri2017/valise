 	// This is to open the modal box when button is pressed
 $(document).ready(function(){
    	$('.modal').modal();
    });

 // a function to push elements when called, instead of using seperate page links

 function addModal(image, story, code, site)